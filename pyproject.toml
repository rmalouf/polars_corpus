[project]
name = "polars-corpus"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
 "jupyterlab>=4.3.6",
 "nltk>=3.9.1",
 "polars>=1.25.2",
]

[tool.uv]
python-preference = "only-managed"
